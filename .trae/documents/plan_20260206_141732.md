I will update the responsive layout of the profile page to ensure the sticker header maintains a better aspect ratio on mobile devices, preventing the "floating tiny stickers" effect while preserving the original arrangement.

### Plan
1.  **Update `templates/user/profile.html`**:
    *   Change the fixed height `h-[380px]` of `#profile-sticker-header` to a responsive height: `h-[220px] sm:h-[300px] lg:h-[380px]`. This ensures the vertical space shrinks proportionally on mobile, matching the scaled-down stickers.
    *   Adjust the content overlap margin `-mt-20` to be responsive: `-mt-12 lg:-mt-20`, ensuring the user avatar and info card are correctly positioned relative to the shorter header on mobile.

### Verification
*   The sticker scaling logic (implemented in the previous step) combined with this container height adjustment will result in a "miniature" but proportionally correct view of the desktop arrangement on mobile devices.
