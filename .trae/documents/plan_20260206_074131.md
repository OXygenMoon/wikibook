# Profile Page Refactor & Sticker System Migration Plan

I will refactor `profile.html` to match the GitHub Redesign concept while migrating the sticker system from the homepage to the profile page, and fixing the specific UI issues you mentioned.

## 1. Migrate Sticker System (Index -> Profile)
-   **Remove from `index.html`**:
    -   Delete `#sticker-canvas`, `#sticker-fab`, `#sticker-drawer` elements.
    -   Remove the `StickerManager` initialization script.
-   **Add to `profile.html`**:
    -   Inject the sticker HTML structure (Canvas, FAB, Drawer) into `profile.html`.
    -   Initialize `StickerManager` in the `profile.html` script block.
    -   *Design Choice*: The sticker canvas will likely be placed as a background layer or a dedicated header section (replacing the current static "Sticker Decoration Area") to allow users to decorate their profile.

## 2. Refactor Profile Layout (GitHub Redesign Style)
I will restructure `profile.html` into a **Sidebar + Bento Grid** layout.

### A. Left Sidebar (Fixed/Sticky)
-   **User Info**: Large Avatar, Name, Username, Bio.
-   **Stats**: Follower/Following counts.
-   **Actions**: "Edit Profile" or "Follow" buttons.
-   **Nav/Social**: Vertical list of links (Overview, Notes, Badges).

### B. Main Content (Bento Grid)
-   **Heatmap Card (Top Wide)**:
    -   **Fix**: Replace the current broken ECharts logic with the **12-month flex layout** from `my_notes.html`.
    -   Ensure it displays all months clearly.
-   **Achievements/Badges Card**:
    -   **Fix**: Adjust CSS grid to support **2 rows** of smaller icons.
    -   **UI**: Improve pagination arrows (make them distinct buttons, centered or on sides).
-   **Pinned/Recent Notes Card**:
    -   Display recent notes in a grid.
-   **Sticker Controls**:
    -   The FAB (Floating Action Button) will be available here to open the sticker drawer.

## 3. Implementation Details
1.  **`index.html`**: Clean up sticker code.
2.  **`profile.html`**:
    -   **Structure**: `<div class="grid grid-cols-12 ...">` -> `col-span-3` (Sidebar) + `col-span-9` (Main).
    -   **Heatmap Script**: Copy `renderMonthChart` and `initCharts` logic from `my_notes.html` to ensure identical rendering.
    -   **Badge CSS**: Update `.badge-page` grid columns to fit more items (smaller size) in 2 rows.
    -   **Sticker Integration**: Ensure `StickerManager` targets the correct page type (likely 'profile' or keep 'wiki' if sharing data, but usually profile stickers are separate. I will assume 'wiki' for now to preserve existing data, or 'profile' if that was the intent. *Correction*: The user said "move the functionality", implying the homepage stickers should now appear on the profile. I will keep the page type consistent or migrate data if needed. I'll use 'wiki' context or check if `app.py` supports 'profile' stickers. *Self-correction*: `UserSticker` has `page_type`. I should probably use `page_type='profile'` for the profile page, but if the user wants to move the *existing* homepage stickers, I might need to keep it 'wiki' or ask. I will default to 'profile' for a clean slate or 'wiki' if they want to see their homepage stickers here. Given the request "move the functionality", I will enable the system on the profile page. I will check `app.py` quickly to see if `page_type` is supported).

## 4. Execution Steps
1.  **Modify `index.html`**: Remove sticker elements.
2.  **Modify `profile.html`**:
    -   Implement new layout.
    -   Embed Sticker HTML.
    -   Update Heatmap JS.
    -   Update Badge Grid CSS.

*Note: I will use the "White Theme" colors as requested (white backgrounds, soft gray borders).*
