# Unify Toast Notification Styling

I will refactor the frontend notification system to use a unified "Toast" style as requested: **bottom-right, slide-up animation, auto-dismiss (3s), and manual close**.

## 1. CSS & Layout Changes (`templates/base.html`)
-   **Reposition Container**: Move `#global-toast-container` from `toast-top toast-center` to `toast-bottom toast-end`.
-   **Styling**:
    -   Use `z-[9999]` to ensure it's on top of everything.
    -   Add `fixed bottom-4 right-4 flex flex-col gap-2`.
    -   Remove `alert-info` etc. from the container itself, apply card styling to children.

## 2. Refactor `showGlobalToast` Function (`templates/base.html`)
-   **Animation**: Add `animate-slide-up-fade` (will need to ensure this animation exists or use standard Tailwind `transition` classes).
    -   Entrance: `transform translate-y-10 opacity-0` -> `translate-y-0 opacity-100`.
    -   Exit: `opacity-0`.
-   **Card Design**:
    -   Use a "Card" look: `bg-base-100/90 backdrop-blur border border-base-content/10 shadow-xl rounded-xl p-4 min-w-[300px]`.
    -   **Layout**: Flex row with icon, content, and close button.
    -   **Close Button**: Add a small "✕" button.
-   **Timing**: Change auto-dismiss from 5000ms to **3000ms**.

## 3. Refactor Flask Flash Message Rendering (`templates/base.html`)
-   Update the Jinja2 loop to use the same HTML structure and CSS classes as the JS function, so server-side messages look identical to client-side ones.
-   Add a small inline script to auto-dismiss these static flash messages after 3s as well.

## 4. Implementation Steps
1.  Modify `templates/base.html`:
    -   Update `#global-toast-container` position.
    -   Rewrite `showGlobalToast` logic.
    -   Rewrite `get_flashed_messages()` loop structure.
    -   Add CSS for slide-up animation if needed (or use Tailwind utilities).

This ensures a consistent, modern notification experience across the application.
