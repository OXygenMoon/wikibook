I will optimize the `profile.html` page according to your UI/UX improvement suggestions. The plan is as follows:

### 1. Visual & Typography Overhaul (De-noising & Refinement)
*   **Background**: Adjust the opacity of the background blobs (`opacity-30`) to prevent visual noise.
*   **Bento Grid Style**: Replace heavy shadows with "Micro-borders" (`border border-base-content/5`) and internal gloss effects for a modern look.
*   **Typography**: Use monospaced/tabular fonts (`font-mono`) for all statistical numbers to ensure professional alignment. Increase the weight of the username (`font-extrabold`).
*   **Avatar**: Remove the hardcoded rainbow background colors and switch to a theme-adaptive style (`bg-primary/10 text-primary`).

### 2. Header Layout Optimization
*   **Badge Position**: Move the currently equipped badge from covering the avatar to the right side of the username (similar to a "Verified" badge).
*   **Social Data**: Visually separate the "Following/Followers" data from the main identity information, using smaller, more subtle styling.

### 3. Stats & Habits Upgrade
*   **Layout**: Reorganize the stats grid to emphasize "Level" and "Study Hours".
*   **Habits Visualization**: Replace the text list with an **ECharts Radar Chart** (Spider Chart) to visually display the "Night Owl", "Early Bird", and "Deep Focus" dimensions.

### 4. Badge Wall Enhancements
*   **Progress Bar**: Add a visual progress bar next to the "Wall of Honor" title to show collection progress.
*   **Fold/Expand**: By default, show only the first 8 unearned badges, with a "Show More" button to expand the rest, keeping the interface clean.

### 5. Recent Activity Timeline
*   **Timeline UI**: Transform the date-box list into a vertical **Timeline** style (connecting lines + dots), emphasizing the content title and weakening the date display (e.g., using relative time or smaller date labels).

### 6. Micro-interactions
*   **Performance**: Replace JS-based click animations with Tailwind's native `active:scale-95` classes.
*   **Hover Effects**: Add subtle border brightening and floating effects on hover.
