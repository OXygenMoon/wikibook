# Profile Page Refactor Plan

I will address the user's three specific requests for `profile.html` to refine the UI and fix functionality issues.

## 1. Sticker Header Gradient (Bottom-Up Fade)
The user wants the background dot pattern ("点阵") to fade out from bottom to top, extending to the edges.
-   **Current**: Likely a static background image or CSS radial-gradient that might not have a fade mask.
-   **Plan**:
    -   Apply a CSS `mask-image` (or `-webkit-mask-image`) to the container that holds the dot pattern.
    -   The mask will be a linear gradient: `linear-gradient(to top, black 0%, transparent 100%)`. This ensures the dots are visible at the bottom and fade out as they go up.
    -   Ensure the container width is `w-full` (already is) to reach edges.

## 2. Fixed Sticker Position (No Scroll)
The user wants stickers to stay fixed relative to the viewport (or header) so they don't scroll with the page content.
-   **Current**: Stickers are likely absolutely positioned within the `#profile-sticker-header`. If the header scrolls, stickers scroll with it.
-   **Interpretation**:
    -   If the user means "parallax" or "fixed background" effect where stickers stay put while content scrolls *over* them: Change `#sticker-canvas` position to `fixed` or use `background-attachment: fixed` logic for the container.
    -   *Correction*: If the header itself scrolls away, "fixed" stickers would overlap the main content below, which looks broken.
    -   *Likely Intent*: The user probably wants the **Header itself** to be sticky/fixed at the top, or the stickers to be fixed *within the header's view* (parallax). Given the description "don't scroll with my scrollbar", `position: fixed` for the canvas is the technical answer.
    -   **Solution**: Set `#sticker-canvas` to `position: fixed; top: 0; left: 0; z-index: -1;`. This places stickers in the background of the viewport. The main content will scroll *over* them (assuming main content has a background color or z-index).
    -   *Refinement*: To prevent stickers from overlapping text when scrolling down, the main content container needs a solid background (white) to cover them up as it scrolls up.

## 3. Fix Heatmap (Match `my_notes.html`)
The user states the heatmap display is incorrect and wants it to match the logic in `my_notes.html` (which uses a 12-month flex layout).
-   **Current**: The profile likely uses a 3-month carousel or broken chart logic.
-   **Plan**:
    -   Replace the current heatmap container and JS with the exact code from `my_notes.html`.
    -   **HTML**: `<div id="heatmap-container" class="flex flex-wrap ...">` (12-month grid).
    -   **JS**: Fetch all data, create 12 separate ECharts instances (one per month), and render them in a flex grid.
    -   **Styling**: Ensure the container fits the profile card width (grid cols might need adjustment for profile layout vs full page layout).

## Implementation Steps
1.  **Modify `profile.html` Style**:
    -   Add `mask-image` to the header background for the dot pattern gradient.
    -   Change `#sticker-canvas` CSS to `fixed`.
2.  **Update `profile.html` HTML/JS**:
    -   Replace Heatmap section with the 12-month logic from `my_notes.html`.
    -   Ensure `echarts.min.js` is loaded.

