# Visitor Banner & Style Enhancement Plan

I will enhance the visitor banner in `index.html` to match your new design requirements and fix the navigation issue.

## 1. Style Enhancements (`index.html`)
-   **Text Styling**: Apply a "colorful" gradient effect to the username (`target_user.username`).
    -   Use `bg-gradient-to-r` with vibrant colors (e.g., `from-pink-500 via-red-500 to-yellow-500`).
    -   Use `bg-clip-text` and `text-transparent` to make the gradient apply to the text itself.
    -   Add `font-extrabold` for better visibility.
-   **Container Styling**: Update the banner container to have a "frosted glass" effect.
    -   Ensure `backdrop-blur-md` (or higher) is used.
    -   Adjust background opacity (`bg-stone-900/80` -> `bg-white/10` or similar for a lighter frosted look, or keep dark but adjust for "frosted"). *User asked for "frosted glass", usually implies a bit more translucency and blur.*
    -   Add a subtle border (`border-white/20`).
-   **Positioning**: Center the banner horizontally and remove the navbar dependency.
    -   Change `top-24` (which was below navbar) to `top-6` or `top-8` to float independently near the top.
    -   Ensure `z-[100]` is high enough to overlap the navbar if needed, or just float above content.

## 2. Navigation Logic (`index.html`)
-   **Close Button**: Update the "Close" (✕) button link.
    -   Currently links to `url_for('index')`.
    -   Change to `url_for('following_users')` (or the specific route for "学友页面") to return the user to their social circle after visiting.

## 3. Implementation Steps
1.  Modify `templates/index.html`:
    -   Update the banner HTML structure and Tailwind classes.
    -   Change the close button `href`.
