# Migrate Badge Equipment Notification to Unified Global Toast

I will update the badge equipment logic in `profile.html` to use the new unified `showGlobalToast` function (from `base.html`) instead of the local implementation. I will also remove the redundant local `showToast` function and CSS styles to avoid conflicts and clean up the code.

## 1. Remove Local Notification Logic (`templates/user/profile.html`)
-   **Remove CSS**: Delete `#toast-container` HTML element and its associated CSS styles (`#toast-container`, `.toast-top`, etc.) around lines 354 and 392-413.
-   **Remove JS Function**: Delete the local `showToast` function definition (lines 392-413).

## 2. Update `quickEquipBadge` Logic (`templates/user/profile.html`)
-   **Replace Call**: Change `showToast('佩戴成功！', 'success')` to `showGlobalToast('佩戴成功！', 'success')`.
-   **Handle Errors**: Change `showToast(data.message || '操作失败', 'error')` to `showGlobalToast(data.message || '操作失败', 'error')`.
-   **Network Error**: Change `showToast('网络错误', 'error')` to `showGlobalToast('网络错误', 'error')`.

## 3. Verify Dependencies
-   The `showGlobalToast` function is defined in `base.html`, which `profile.html` extends, so it is available globally.

## 4. Implementation Steps
1.  Modify `templates/user/profile.html`:
    -   Delete the old `toast-container` div.
    -   Delete the local `showToast` JS function.
    -   Update `quickEquipBadge` to use `showGlobalToast`.

This will ensure badge equipment notifications appear in the bottom-right corner with the unified style.
