I will implement the wiki customization feature to allow users to set the background color, foreground color (text/pattern), and texture pattern for Wiki cards.

### Plan
1.  **Update Data Model (`app.py`)**:
    *   Add `bg_color` (default: `#ffffff`), `fg_color` (default: `#1f2937`), and `pattern` (default: `pattern-none`) columns to the `Wiki` model.
    *   Add database migration logic to automatically add these columns if they don't exist.

2.  **Add CSS Patterns (`templates/base.html`)**:
    *   Define CSS classes for common patterns (e.g., `pattern-dots`, `pattern-grid`, `pattern-lines`, `pattern-diagonal`, `pattern-zigzag`) using `background-image` gradients and `currentColor` to dynamically match the foreground color.

3.  **Update Routes (`app.py`)**:
    *   Update `create_wiki` route to retrieve and save these new fields.
    *   Update `edit_wiki` route to retrieve and save these new fields.

4.  **Update Admin Templates (`templates/admin/create_wiki.html` & `templates/admin/edit_wiki.html`)**:
    *   Add an "Appearance Settings" (外观设置) section to the forms.
    *   Add Color Pickers for Background and Foreground colors.
    *   Add a visual Pattern Selector (using radio buttons styled as previews) to choose the texture.

5.  **Update Frontend (`templates/index.html`)**:
    *   Update the Wiki card styling to apply the selected `background-color`, `color`, and pattern CSS class.
    *   Ensure the avatar and text remain visible and legible (using the selected foreground color).

### Implementation Steps
1.  Modify `app.py` to add model fields and migration logic.
2.  Modify `app.py` to update route handlers.
3.  Modify `templates/base.html` to add CSS patterns.
4.  Modify `templates/admin/create_wiki.html` to add the UI.
5.  Modify `templates/admin/edit_wiki.html` to add the UI.
6.  Modify `templates/index.html` to render the customized cards.
