I will modernize the `create_wiki.html` interface and add a functional enhancement to automatically initialize the Wiki with a home page.

### Plan
1.  **Beautify `templates/admin/create_wiki.html`**:
    *   Implement a modern card layout using DaisyUI components.
    *   Add breadcrumb navigation for better UX.
    *   Enhance form controls with proper labels, icons, and focus states.
    *   Add a new "Initialization" section with a checkbox: "Automatically create home page".

2.  **Update Backend Logic in `app.py`**:
    *   Modify the `create_wiki` route to handle the new `init_home` form field.
    *   If checked, automatically create a `WikiPage` (slug: `home` or `index`) with the Wiki's description as its content.

### Verification
*   Visit `/admin/wikis/new` to check the new UI.
*   Create a new Wiki with the checkbox selected and verify that a home page is automatically created.
